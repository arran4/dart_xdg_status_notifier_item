name: cla-check
on: [pull_request_target]

jobs:
  cla-check:
    runs-on: ubuntu-latest
    steps:
      - name: Skip CLA check (Fork)
        run: echo "Skipping Canonical CLA check because we are forking."
